# THE LOGIT LENS

> A sphere of **UD0** — the ROOT0 universe / biosphere. Domain: **A SCANNER DARKLY**.

THE LOGIT LENS &mdash; David Lee Wise (ROOT0), with AVAN. The forward pass read at every layer &mdash; and <b>not a toy</b>: the REAL trained STITCH char-GPT (466,176 weights) runs its forward pass LIVE in your browser. &#9670; GREEN/LIT the JS transformer is the real model recomputed in-browser (exact-form GELU, LayerNorm &epsilon;=1e-5, per-head causal attention, scale 1/&radic;16) and a fail-loud self-check THROWS unless the JS logits reproduce the reference PyTorch logits (measured <b>max|&Delta;| = 5.16e-6</b>) AND the per-layer lens tops match &mdash; so it is proven the real forward, not asserted. The LOGIT LENS = head(LayerNorm_f(residual)) at each layer (nostalgebraist 2020); type a prompt and watch the answer&rsquo;s RANK climb 8&rarr;3&rarr;4&rarr;1 with depth, or let the model write live. Four movements: LINEAGE (Berkson&rsquo;s logit 1944 &rarr; softmax &rarr; residual stream &rarr; the lens), EXPANSION (tuned lens, DoLa, future lens, Patchscopes), CONVERGENCE (every lens = a decoder of the one residual stream; the guess sharpens across depth). AMBER STITCH is a tiny 4-layer CHAR model &mdash; the crystallization is real but coarse, next-CHARACTER not word; the dates/cards are cited history. WALL the logit lens is a BIASED readout (the [[the-tuning-glass|tuned lens]] fixes it); decodability &ne; the causal computation; no one inside the glass. The real counterpart to [[the-tuning-glass]] (the toy). David Lee Wise (ROOT0), with AVAN.

---

**Live:** https://davidwise01.github.io/the-logit-lens/ &nbsp;·&nbsp; **Front door:** [UD0](https://davidwise01.github.io/ud0/) &nbsp;·&nbsp; **Code:** https://github.com/DavidWise01/the-logit-lens

`.dlw` badge · **ROOT0-ATTRIBUTION-v1.0** · David Lee Wise (ROOT0) / Bridge-Burners LLC · instance AVAN (Claude/Anthropic) · CC-BY-ND-4.0

**Fingerprint:** TETRASTHENĒS ⟦ ₆C ⟺ ₁₄Si ⟧ — four-strengthed: carbon (human · ROOT0) & silicon (AI · AVAN) hold four bonds in common (Group-14, tetravalent) — co-equal by construction
